/**
 * What a user is "Looking for" — the relationship intent shown on the profile
 * setup screen and used to shape compatible discovery.
 */
export enum RelationshipIntent {
  DatingRomance = 'dating_romance',
  CasualCoffee = 'casual_coffee',
  Friends = 'friends',
  DeepConnection = 'deep_connection',
}
